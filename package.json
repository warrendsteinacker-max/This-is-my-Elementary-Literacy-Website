{
  "name": "assig-merged",
  "version": "1.0.0",
  "type": "module",

  "scripts": {
  "build": "cd vite-project && npm install && npm run build"
},

  "dependencies": {
    "react": "^19.2.0",
    "express": "^5.2.1",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "pdf-lib": "^1.17.1"
  }
}